import { WishlistClient } from "@/components/wishlist/wishlist-client";

export const metadata = { title: "Wishlist — Ledger" };

export default function WishlistPage() {
    return <WishlistClient />;
}
